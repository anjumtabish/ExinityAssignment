{
    "fields": {
        "project": {
            "key": "${projectKey}"
        },
        "labels": ["${labels}"],
        "summary": "${summary}",
        "description": "${description}",
        "issuetype": {
            "name": "${issueType}"
        }
    }

}